/** @internal */
export const TERMINAL_CLASSES = Object.freeze({
  ROOT: 'k-terminal',
  HEADER: 'k-terminal-header',
  WINDOW_CTRLS: 'k-terminal-ctrls',
  HISTORY: 'k-terminal-history',
  INPUT: 'k-terminal-input',
})

/** @internal */
export const TERMINAL_CTRLS = Object.freeze({
  FORM: 'k-terminal-form',
  INPUT: 'k-terminal-input',
})

export const TERMINAL_COMMANDS = Object.freeze({
  CLEAR: 'clear',
  CLS: 'cls',
})
