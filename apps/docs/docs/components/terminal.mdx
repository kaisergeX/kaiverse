---
sidebar_position: 1
---

import {Terminal} from '@kaiverse/k/ui'

# Terminal

Terminal component

## Import

```js
import {Terminal} from '@kaiverse/k/ui'
```

Try command `hello` and then `clear` in the terminal below:

<Terminal
  title="Terminal"
  greeting={`T
E  __      __        __                    
R /  \\    /  \\ ____ |  |   ____  ____   _____   ____  
M \\   \\/\\/   // __ \\|  | _/ ___\\/  _ \\ /     \\_/ __ \\ 
I  \\        /\\  ___/|  |_\\  \\__(  <_> )  Y Y  \\  ___/ 
N   \\__/\\  /  \\___  >____/\\___  >____/|__|_|  /\\___  >
A        \\/       \\/          \\/            \\/     \\/
L`}
  commandHandler={(command, println) => {
    if (command === 'hello') {
      println('Hello world👋!')
      return true
    }
    return false
  }}
  style={{maxHeight: '30dvh'}}
/>

## Usage

```jsx title="Inside your component"
<Terminal title="Terminal UI" greeting="Terminal playground" />
```

<Terminal title="Terminal UI" greeting="Terminal playground" />

## Handle Command

You can provide your handler by using `commandHandler` [prop](#props).
Your function should returns `true` if the command is manually handled. Otherwise, commands will be executed by default handler.

`commandHandler` prop also has a built-in `println` function (2nd parameter), you can call it to print something to the terminal.

### Default Commands

- `clear` - clear the terminal
- Others - println: "command not found".

## Greatings

The `greeting` prop accept a `ReactNode`.

<Terminal
  title="¯\_(ツ)_/¯"
  greeting={
    <img width="120" height="120" src="https://i.redd.it/fktuppkre7p51.gif" alt="richroll" />
  }
/>

## Special Types

| Name        | Type                      | Description               |
| :---------- | :------------------------ | :------------------------ |
| `PrintlnFn` | `(input: string) => void` | Print the inputted string |

## Props

| Name                | Type                                               | Default | Description                                                                                                                                |
| :------------------ | :------------------------------------------------- | :------ | :----------------------------------------------------------------------------------------------------------------------------------------- |
| `title`             | `string`                                           | —       | Terminal window title.                                                                                                                     |
| `greeting`          | `ReactNode`                                        | —       | Greating section. It will be printed on top of terminal's history section.                                                                 |
| `commandPrefix`     | `string`                                           | `'$'`   |                                                                                                                                            |
| `commandHandler`    | `(command: string, println: PrintlnFn) => boolean` | —       | Provide custom command handler. Returns `true` if the command is manually handled, otherwise commands will be executed by default handler. |
| `...htmlAttributes` | `Omit<HTMLAttributes<HTMLDivElement>, 'onClick'>`  | —       | Others HTML attributes.<br/><small>*Note*: `onClick` event handler is not supported.</small>                                                 |
